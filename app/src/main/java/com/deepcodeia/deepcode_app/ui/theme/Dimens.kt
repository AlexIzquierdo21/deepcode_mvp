package com.deepcodeia.deepcode_app.ui.theme

import androidx.compose.ui.unit.dp
import androidx.compose.ui.unit.sp

// Objeto centralizado para definir dimensiones y tamaños usados en toda la app
object Dimens {
    val ScreenPadding = 24.dp      // Margen general de las pantallas
    val TileRadius = 16.dp         // Radio de los bordes de las tarjetas (tiles)
    val TileHeight = 112.dp        // Altura estándar de las tarjetas del menú
    val ButtonHeight = 52.dp       // Altura estándar de los botones
    val TitleSize = 28.sp          // Tamaño de fuente para títulos principales
    val ButtonRadius = 24.dp       // Radio de los bordes de los botones
}
