package com.deepcodeia.deepcode_app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Paleta de colores personalizada para DeepCodeApp
 * Define todos los colores base usados en el tema oscuro de la aplicación
 **/

// Colores de fondo y superficies
val DeepBg = Color(0xFF0B0B0B)              // Fondo principal (negro profundo)
val DeepSurface = Color(0xFF161616)         // Superficies (tarjetas, paneles)
val DeepSurfaceVariant = Color(0xFF202020)  // Variante más clara de las superficies

// Colores de marca y acentos
val DeepNeon = Color(0xFF20FF00)            // Color principal (verde neón característico)
val DeepElectricBlue = Color(0xFF00E5FF)    // Azul eléctrico (color de acento secundario)

// Colores de UI
val DeepBorder = Color(0xFF2E2E2E)          // Bordes y contornos sutiles

// Colores de texto
val DeepOnBackground = Color(0xFFEFEFEF)    // Texto e iconos sobre el fondo principal
val DeepTextSecondary = Color(0xFFBDBDBD)   // Texto secundario o desactivado

// Color para retos completados (verde oscuro con tinte neón)
val DeepCompletedChallenge = Color(0xFF1A3D00)

// Colores para chips con significado visual
val DeepChipLanguage = Color(0xFFFFAA00) // Amarillo - Informativo
val DeepChipLevel = Color(0xFFFF3D00)   // Rojo - Nivel/Dificultad



